<%-include('partials/header')%>
<%-include('partials/homenavbar')%>    
    <div class="container">
    <h1>Seja bem vindo ao blog!</h1>
    <% articles.forEach(article => {%>

        <div class="card">

            <div class="card-header">
                <h3 style="color:white;"><%= article.title %></h3>
            </div>

            <div class="card-body">
                <a class="btn btn-sucess" href="/<%=article.slug%>">Ler Artigo</a>
            </div>

        </div>
        <br>

    <%})%>
    <div class="row">
        <div class="col col-12"><a href="/articles/page/2" style="float: right;">Próxima>></a></div>
    </div>
    </div>
</html>
<%-include('partials/footer')%>